// Compatibility import retained for StudioNext and historical tests.
// All device decisions are delegated to the single v140 viewport authority.
export {
  COMPACT_MAX as SMALL_MAX,
  MODE_EVENT,
  RELEASE,
  currentStudioDeviceMode,
  detectStudioDeviceMode,
} from "./studio-device-mode-v140.js";
