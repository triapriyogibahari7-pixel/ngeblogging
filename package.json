{
  "name": "ngeblogging",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start:api": "node api/server.mjs",
    "check": "npm test && vite build",
    "test": "node --test tests/*.test.mjs",
    "test:production": "node --test tests/*.test.mjs && node scripts/validate-production.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.110.7",
    "lucide-react": "1.25.0",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "vite": "8.1.5"
  }
}
