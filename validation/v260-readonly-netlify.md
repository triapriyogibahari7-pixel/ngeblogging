# v260 read-only build validation

This branch only triggers a deploy preview for the current `main` state after removing build-time source mutation and the unproven v260 post-bundle service-worker mutation.
