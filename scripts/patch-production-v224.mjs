// Temporary diagnostic no-op. The v224 source files remain on the branch,
// but the production patch chain does not mutate Studio or service-worker state
// in this commit. This isolates whether the Netlify failure is caused by v224.
console.log("V224_DIAGNOSTIC_NOOP");
