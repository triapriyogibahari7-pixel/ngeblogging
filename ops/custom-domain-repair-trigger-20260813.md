# Custom domain repair trigger

This marker intentionally triggers the existing `Ngeblogging custom domain repair` workflow on `main` so the Ubuntu 24.04 repair runner executes after the runner migration.
