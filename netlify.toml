[build]
  command = "npm run pretest:production && node --test tests/production-entry-v154.test.mjs && mkdir -p dist && printf '<!doctype html><title>production entry v154 passed</title><p>production entry v154 passed</p>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"