[build]
  command = "npm run test:production && mkdir -p dist && printf '<!doctype html><title>all production contracts pass</title>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
