[build]
  command = "V192_DIAGNOSTIC_STAGE=onboarding node scripts/patch-production-v192.mjs && mkdir -p dist && printf 'v192 onboarding patch passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
