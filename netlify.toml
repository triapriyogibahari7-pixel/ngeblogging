[build]
  command = "bash -lc 'set -e; sed -i \"/patch-production-v216.mjs/d\" scripts/patch-service-worker-v179.mjs; npm run test:production; mkdir -p dist; printf \"<!doctype html><meta charset=utf-8><title>v215 production baseline passed</title>\" > dist/index.html'"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
