[build]
  # TEMP v195 draft diagnostic: check exact publish anchor expected by v195.
  command = "node scripts/patch-service-worker-v179.mjs && grep -Fq 'function publishActiveSite(site, userId = \"\")' src/StudioOnboardingGate.jsx && grep -Fq 'rememberActiveSiteV192(site, userId);' src/StudioOnboardingGate.jsx && mkdir -p dist && printf '<!doctype html><div id=\"root\"></div>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"