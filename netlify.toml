[build]
  # Temporary PR diagnostic: probe v190 service-worker assertions. Restore to `npm run build` before merge.
  command = "node scripts/patch-auth-callback-v162.mjs && node scripts/patch-service-worker-v179.mjs && CHECK_GROUP=second node scripts/probe-v190-sw-assertions.mjs && mkdir -p dist && printf '<!doctype html><title>v190 sw probe</title>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"