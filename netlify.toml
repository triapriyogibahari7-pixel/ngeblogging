[build]
  command = "V191_DIAGNOSTIC_SKIP=1 node scripts/patch-service-worker-v179.mjs && mkdir -p dist && printf 'v179-v190 chain passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
