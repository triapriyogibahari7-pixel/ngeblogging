[build]
  # Temporary PR diagnostic: identify exact failing patch. Restore to `npm run build` before merge.
  command = "SKIP_V192=1 node scripts/patch-auth-callback-v162.mjs && DIAG_MATCH='V182_' SKIP_V192=1 node scripts/probe-v192-chain-error.mjs"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
