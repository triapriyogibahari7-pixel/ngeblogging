[build]
  command = "PATCH_DIAGNOSTIC_STOP=v188 node scripts/patch-service-worker-v179.mjs && mkdir -p dist && printf 'v179-v188 chain passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
