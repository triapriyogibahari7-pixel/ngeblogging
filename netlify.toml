[build]
  # Temporary PR diagnostic: v186 baseline only. Restore to `npm run build` before merge.
  command = "node scripts/patch-auth-callback-v162.mjs && node scripts/patch-production-data-v186.mjs && mkdir -p dist && printf '<!doctype html><title>v186 baseline diagnostic</title>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
