[build]
  # Diagnostic: compile only the browser application. If this fails, the v288
  # source/import graph itself is broken. Service-worker and infrastructure
  # validation are deliberately excluded from this preview probe.
  command = "npx vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22.12.0"
