[build]
  command = "PATCH_DIAGNOSTIC_STOP=v186 node scripts/patch-service-worker-v179.mjs && mkdir -p dist && printf 'v179-v186 chain passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
