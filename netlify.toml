[build]
  command = "V183_DIAGNOSTIC_STOP=service-worker node scripts/patch-service-worker-v179.mjs && mkdir -p dist && printf 'v183 service worker passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
