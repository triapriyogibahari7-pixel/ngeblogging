[build]
  command = "npm run patch:studio && mkdir -p dist && printf '<!doctype html><title>v202 patch stage ok</title>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
