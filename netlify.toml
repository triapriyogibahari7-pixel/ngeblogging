[build]
  command = "npm run predev && node --test tests/netlify-production-publisher-v156.test.mjs && mkdir -p dist && printf '<!doctype html><html><body><h1>NETLIFY_PUBLISHER_V156_OK</h1></body></html>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
