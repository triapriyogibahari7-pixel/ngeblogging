[build]
  command = "bash scripts/v192-regression-group.sh recent-b && mkdir -p dist && printf 'v189-v192 regression group passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
