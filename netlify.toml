[build]
  command = "node scripts/patch-service-worker-v179.mjs && node --test --test-name-pattern='production deployment' tests/studio-data-bootstrap-v192.test.mjs && mkdir -p dist && printf 'v192 deployment gate assertion passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
