[build]
  # Temporary PR diagnostic: full production regression suite only. Restore to `npm run build` before merge.
  command = "npm run test:production && mkdir -p dist && printf '<!doctype html><title>production regression</title>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"