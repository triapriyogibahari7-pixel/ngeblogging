[build]
  command = "node --test tests/studio-completion-v151.test.mjs && mkdir -p dist && printf '<!doctype html><title>completion v151 passes</title>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
