[build]
  # Temporary PR diagnostic: classify exact v183 failure. Restore to `npm run build` before merge.
  command = "SKIP_V192=1 node scripts/patch-auth-callback-v162.mjs && DIAG_MATCH='V183_(BOOTSTRAP|STUDIO|SELECT_SITE)' SKIP_V192=1 node scripts/probe-v192-chain-error.mjs"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
