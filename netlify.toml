[build]
  command = "bash -lc 'set -e; npm run test:production; mkdir -p dist; printf \"<!doctype html><meta charset=utf-8><title>v216 test passed</title>\" > dist/index.html'"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
