[build]
  # Temporary PR diagnostic: v192 patch only. Restore to `npm run build` before merge.
  command = "node scripts/patch-auth-studio-bootstrap-v192.mjs && mkdir -p dist && printf '<!doctype html><title>v192 diagnostic</title>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
