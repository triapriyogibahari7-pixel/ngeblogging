[build]
  # Production build wajib menjalankan patch autentikasi PKCE, regresi lengkap,
  # Vite, pemeriksaan Worker, dan redirect writer melalui satu authority.
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
