[build]
  command = "npm run test:production && mkdir -p dist && printf '<!doctype html><title>production regression passed</title><p>production regression passed</p>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"