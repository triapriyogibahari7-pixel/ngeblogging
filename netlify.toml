[build]
  command = "bash scripts/v192-regression-group.sh recent-a && mkdir -p dist && printf 'v179-v186 regression group passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
