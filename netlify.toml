[build]
  # Temporary PR diagnostic: all v190 checks except service-worker identity. Restore to `npm run build` before merge.
  command = "node scripts/patch-auth-callback-v162.mjs && node scripts/patch-service-worker-v179.mjs && node --test --test-name-pattern='^(?!v190 service worker identity)' tests/studio-real-device-v190.test.mjs && vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"