[build]
  command = "npm run predev && node --test tests/auth-production-v153.test.mjs && mkdir -p dist && printf '<!doctype html><html><body><h1>AUTH_PRODUCTION_V153_OK</h1></body></html>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
