[build]
  command = "npm run verify:v177 && npm run verify:v178 && npm run verify:v179 && npx vite build && node scripts/write-netlify-redirects.mjs"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
