[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[context.deploy-preview]
  command = "node --test --test-name-pattern='v172 makes apex' tests/production-custom-domain-v172.test.mjs && npx vite build && node scripts/write-netlify-redirects.mjs"
