[build]
  # Temporary PR diagnostic. Restore to `npm run build` and delete diagnostic script before merge.
  command = "node scripts/patch-auth-callback-v162.mjs && node scripts/diagnose-v192-chain.mjs"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
