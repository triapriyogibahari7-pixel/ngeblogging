[build]
  # Temporary PR diagnostic: complete v190 regression on latest branch. Restore to `npm run build` before merge.
  command = "node scripts/patch-auth-callback-v162.mjs && node scripts/patch-service-worker-v179.mjs && node --test tests/studio-real-device-v190.test.mjs && vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"