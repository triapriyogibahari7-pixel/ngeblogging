[build]
  command = "npm run pretest:production && node --test tests/auth-production-v153.test.mjs && mkdir -p dist && printf '<!doctype html><title>auth v153 passed</title><p>auth v153 passed</p>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"