[build]
  command = "bash -lc 'set -e; node scripts/patch-service-worker-v179.mjs; mkdir -p dist; printf \"<!doctype html><meta charset=utf-8><title>v216 patch chain passed</title>\" > dist/index.html'"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
