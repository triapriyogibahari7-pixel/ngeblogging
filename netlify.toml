[build]
  # Temporary PR diagnostic: confirm failing v190 test number 10. Restore to `npm run build` before merge.
  command = "node scripts/patch-auth-callback-v162.mjs && node scripts/patch-service-worker-v179.mjs && DIAG_MATCH='not ok 10 -' node scripts/probe-v190-failure.mjs && mkdir -p dist && printf '<!doctype html><title>v190 TAP classifier</title>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"