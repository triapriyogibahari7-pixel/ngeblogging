[build]
  command = "mkdir -p dist && (npm run predev && node scripts/patch-interaction-precision-v183.mjs) > dist/index.html 2>&1 || true"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"