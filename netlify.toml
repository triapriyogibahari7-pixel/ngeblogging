[build]
  # Temporary PR diagnostic: v190 Nara/data/onboarding/release checks. Restore to `npm run build` before merge.
  command = "node scripts/patch-auth-callback-v162.mjs && node scripts/patch-service-worker-v179.mjs && node --test --test-name-pattern='Nara small and medium remain|Studio data uses|transient onboarding continuity|release contract covers' tests/studio-real-device-v190.test.mjs && vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"