[build]
  command = "bash scripts/v192-regression-group.sh recent-b2 && mkdir -p dist && printf 'v191-v192 regression pair passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
