[build]
  # Temporary PR diagnostic: isolate patch-chain + Vite compilation from legacy regression tests.
  # This must be restored to `npm run build` before merge.
  command = "node scripts/patch-auth-callback-v162.mjs && node scripts/patch-service-worker-v179.mjs && vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
