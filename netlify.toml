[build]
  command = "npm run test:production && mkdir -p dist && printf '<!doctype html><title>v202 production regressions ok</title>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
