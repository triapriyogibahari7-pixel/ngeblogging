[build]
  command = "npm run test:production && npx vite build && node --check cloudflare/worker-v69.mjs && node scripts/write-netlify-redirects.mjs"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
