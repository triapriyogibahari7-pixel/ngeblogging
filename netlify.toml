[build]
  command = "V190_DIAGNOSTIC_SKIP=1 node scripts/patch-service-worker-v179.mjs && mkdir -p dist && printf 'v179-v189 chain passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
