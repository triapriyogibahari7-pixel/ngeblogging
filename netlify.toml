[build]
  command = "npm run predev && node --test tests/production-entry-v154.test.mjs && mkdir -p dist && printf '<!doctype html><html><body><h1>PRODUCTION_ENTRY_V154_OK</h1></body></html>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
