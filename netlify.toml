[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[context.deploy-preview]
  command = "node --test tests/auth-production-v153.test.mjs && npx vite build && node scripts/write-netlify-redirects.mjs"
