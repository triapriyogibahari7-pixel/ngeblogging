[build]
  command = "V192_DIAGNOSTIC_STAGE=onboarding node scripts/patch-service-worker-v179.mjs && mkdir -p dist && printf 'v179-v191 plus v192 onboarding passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
