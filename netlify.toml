[build]
  # Temporary PR diagnostic: locate exact v183 verify marker. Restore to `npm run build` before merge.
  command = "SKIP_V192=1 node scripts/patch-auth-callback-v162.mjs && DIAG_MATCH='V183_VERIFY_FAILED:src/NaraAssistant' SKIP_V192=1 node scripts/probe-v192-chain-error.mjs"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
