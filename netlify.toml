[build]
  command = "V183_DIAGNOSTIC_STOP=nara node scripts/patch-service-worker-v179.mjs && mkdir -p dist && printf 'v183 nara passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
