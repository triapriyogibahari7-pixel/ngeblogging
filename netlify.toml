[build]
  command = "npm run verify:v177 && mkdir -p dist && printf '<!doctype html><title>v177 regression passed</title>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
