[build]
  # TEMP v195 draft diagnostic: patch-chain only. Restore before merge.
  command = "node scripts/patch-service-worker-v179.mjs && mkdir -p dist && printf '<!doctype html><div id=\"root\"></div>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"