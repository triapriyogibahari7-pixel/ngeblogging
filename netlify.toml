[build]
  command = "bash scripts/v192-regression-group.sh recent && mkdir -p dist && printf 'v179-v192 regression group passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
