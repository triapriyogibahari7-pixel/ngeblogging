[build]
  command = "npm run predev && node --test tests/legacy-worker-entry-v157.test.mjs && mkdir -p dist && printf '<!doctype html><html><body><h1>LEGACY_WORKER_ENTRY_V157_OK</h1></body></html>' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
