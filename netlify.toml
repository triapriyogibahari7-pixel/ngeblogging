[build]
  command = "bash scripts/v192-regression-group.sh recent-b1 && mkdir -p dist && printf 'v189-v190 regression pair passed' > dist/index.html"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
