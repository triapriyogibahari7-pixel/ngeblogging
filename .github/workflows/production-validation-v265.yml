name: Production Validation v268

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: production-validation-v268-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Test and build Ngeblogging
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node 22
        uses: actions/setup-node@v4
        with:
          node-version: "22.12.0"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate v268 responsive shell contract
        run: node --test tests/studio-final-device-authority-v268.test.mjs

      - name: Production regression tests and build
        run: npm run build

      - name: Assert v268 source and cache markers
        run: |
          test -f src/studio-screenshot-authority-v265.js
          test -f src/studio-theme-layout-v264.js
          test -f src/studio-shell-v265-final-hotfix.css
          test -f src/studio-editor-navigation-v266.js
          test -f src/studio-sidebar-single-toggle-v267.js
          test -f src/studio-final-device-authority-v268.css
          test -f tests/studio-fixed-chrome-v267.test.mjs
          test -f tests/studio-final-device-authority-v268.test.mjs
          test -f public/release-v267.json
          test -f public/release-v268.json
          test -f scripts/patch-service-worker-v267.mjs
          test -f scripts/patch-service-worker-v268.mjs
          grep -q 'studio-sidebar-single-toggle-v267.js' src/Studio.jsx
          grep -q 'studio-final-device-authority-v268.css' src/studio-sidebar-single-toggle-v267.js
          grep -q 'patch-service-worker-v268.mjs' scripts/patch-service-worker-v267.mjs
          grep -q 'studio-final-device-cache-v268' public/sw.js
          grep -q 'UI_PATCH_RELEASE_V268' public/sw.js
