name: Ngeblogging v219 runner probe

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Confirm runner starts
        run: echo "NGEBLOGGING_V219_RUNNER_STARTED"
