name: Ngeblogging production route cutover v184 — Studio v191/v192/v195/v196/v197/v205

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Historical workflow runner smoke
        run: |
          echo "NGE_BLOGGING_HISTORICAL_WORKFLOW_RUNNER_SMOKE_OK"
          node --version
