# Main Merge Event v172

This operational marker creates a normal pull-request merge event on `main` in case Cloudflare is configured to treat `main` as the production branch.

The application authority remains unchanged:

- `2026.07.30-production-custom-domain-v172`
- exact Worker Domains for apex and `www`
- tenant wildcard route preserved
- mobile/public layout v171
- onboarding and site policy v169
- authentication/editor v162

This marker changes no application data or user content.
