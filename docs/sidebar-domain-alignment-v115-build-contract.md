# Build contract — Sidebar Domain alignment v115

The full build must include the v115 runtime and CSS through `studio-domain-single-authority-v112.js`, and the regression suite must keep the old inline margin/position authority from returning.
